# Edify

git clone [url]
cd [locate cloned repo]
git checkout -b [create new branch to prevent overwritting main]
git add [select or select all '.']
git commit -m [apply changes on local repo]
git push origin [apply changes on remote repo]
git push upstream [apply changes on remote repo] //use upstream for those who are forking, to apply changes to main fork,
